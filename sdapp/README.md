```
python -m uvicorn server:app --host 127.0.0.1 --port 3010 --reload
```

```
install comfyui
enable Dev Mode
set port 8282
add node 746 ComfyUI-TextOverlay
add model 99 v1-5-pruned-emaonly.ckpt
```